{
	"validator": {
		"$jsonSchema": {
			"bsonType": "object",
			"required": ["ownerId", "name", "duration", "color"],
			"properties": {
				"ownerId": {
					"bsonType": "string",
					"description": "ID of the therapist that created the type of appointment, is required"
				},
				"name": {
					"bsonType": "string",
					"description": "name of the type of appointment, is required"
				},
				"duration": {
					"bsonType": "string",
					"description": "name of the type of appointment, is required"
				},
				"color": {
					"bsonType": "string",
					"description": "color in hexadecimal (starting by #) of the type of appointment, is required"
				},
				"description": {
					"bsonType": "string",
					"description": "description of the type of appointment, is not required"
				}
			}
		}
	}
}